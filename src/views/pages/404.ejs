<!DOCTYPE html>
<html lang="pt">
    <%- include("../partials/_head") %>
    <body class="theme-dark d-flex flex-column min-vh-100">
        <!-- Header opcional -->
        <%- include("../partials/_header") %>

        <main class="container my-5">
            <div class="text-center p-5 rounded-4 shadow bg-white">
                <h1 class="mb-3">Página não encontrada</h1>
                <p class="lead mb-4">A página que procuras não existe.</p>
                <a href="/" class="btn btn-primary">Voltar à página inicial</a>
            </div>
        </main>

        <%- include("../partials/_footer") %>
    </body>
</html>
